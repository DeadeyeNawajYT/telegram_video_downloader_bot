services:
  - type: web
    name: telegram-video-downloader-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python telegram_video_downloader_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
